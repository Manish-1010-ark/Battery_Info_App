<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:gravity="center_vertical"
    android:background="@drawable/notification_bg"
    android:clickable="false"
    android:focusable="false"
    >

    <TextView
        android:id="@+id/notification_battery_percentage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔋 --%"
        android:textStyle="bold"
        android:textSize="11sp"
        android:layout_marginEnd="12dp"
        android:contentDescription="Battery percentage" />

    <TextView
        android:id="@+id/notification_time_remaining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Time remaining"
        android:text="⌛ --"
        android:textSize="11sp"
        android:layout_marginEnd="12dp"
        />

    <TextView
        android:id="@+id/notification_charging_power"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⚡ -- W"
        android:textSize="11sp"
        android:layout_marginEnd="12dp"
        android:contentDescription="Charging power" />

    <TextView
        android:id="@+id/notification_temperature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🌡️ -- °C"
        android:textSize="11sp"
        android:layout_marginEnd="12dp"
        android:contentDescription="Battery temperature" />

</LinearLayout>
